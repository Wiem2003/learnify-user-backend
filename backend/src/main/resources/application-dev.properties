# Development profile: reset DB on startup to fix "Tablespace exists" / missing tables.
# Use: -Dspring.profiles.active=dev (or set in IDE). Set back to default profile after first run to keep data.
learnivo.reset-db-on-startup=true
